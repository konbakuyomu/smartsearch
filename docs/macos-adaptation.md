# macOS 适配阶段记录

分支：`codex/macos-native-adaptation`。上游：`konbakuyomu/smartsearch`；阶段提交推送至 `cr-zhichen/smartsearch`，全部适配完成且维护者验收后才向上游创建 PR。

## 分阶段交付

0. **安装基础**：保存已验证的 DMG 签名、安装布局、成品检查和 `.DS_Store` 忽略规则。
1. **启动与系统集成**：复现并修复 Swift 客户端启动超时，修复 Dock 和菜单栏图标，确认真实应用启动与关闭/重新打开路径。
2. **原生界面**：以 UTM 中正在运行的 Windows 版为功能依据，参考 DJOneHub / Codex Tweaks 的 macOS 原生层级；逐页调整导航、概览、服务商、搜索、活动、Skills 和设置。
3. **验收**：构建可安装产物并交给用户检查语言、主题和窗口尺寸。按照用户最新要求，不再自动操作主机或虚拟机、不再替换已安装的 App；实际界面与交互由用户验收。获准后再创建上游 PR。

每个阶段都单独提交并推送。不会把进程存在、后端单独 smoke 或编译成功等同于完整 App 验收。

## 设计方向

- 保留 Windows 已有功能和工作流，使用 macOS 的原生 `NavigationSplitView`、选择态列表、工具栏和系统语义色。按用户后续指定，以 Codex Tweaks 的页面布局为主要参照；不再把复杂工作页整体放入分组 Form。
- 使用 SwiftUI Scene 管理主窗口，保留当前 SDK 的原生系统材质；兼容原有 macOS 13 最低版本，不用自建 AppKit 窗口取代原生层级。
- Dock 使用应用品牌图标，菜单栏使用紧凑模板符号；不把大尺寸彩色应用图标直接用于菜单栏。
- 状态表达有实际依据：启动、连接失败、重试和就绪清楚区分，不隐藏错误、不通过延长超时掩盖通信问题。
- 不编写 UI 单元测试；以实际构建、协议进程集成检查和原生窗口验收为依据。

## 阶段 0

已完成，验收证据见 [macOS 打包修复验证](macos-packaging-validation.md)。后续的启动握手和 App 页面表现属于下一阶段，尚不能由打包签名校验代替。

## 阶段 1

- Release 模式直接编译原有 Swift 通信客户端，连接实际安装的 Python 后端；12 次冷启动中 4 次出现握手超时。后端单独启动约 0.2 秒，不能靠增加超时解决。
- 旧实现为每个 stdout 数据块新建一个 `Task`，不保证数据块抵达 actor 的顺序。改用 `AsyncStream` 和一个消费者，按管道顺序组装 JSON；同时在退出时完成流并取消读取任务，取消的超时任务不再继续执行。
- 修改后同样 12 次握手全部成功。新增 `mise run desktop:macos:backend-check /path/to/backend`，以 Release 优化编译生产客户端，检查 12 次启动/停止/重连，每次还发送 4 个并发完整状态请求和 ping；全部通过。使用隔离配置，不调用付费搜索。这是实际进程集成检查，不是 UI 单元测试。
- Dock 从应用包显式读取品牌图标；菜单栏使用系统 `magnifyingglass` 模板符号，避免彩色应用图像的固有尺寸撑开菜单栏。
- 主窗口保持 SwiftUI Scene 管理，改为单窗口并设定默认尺寸；侧边栏采用原生选择态，连接状态移至底部；窗口关闭后隐藏，菜单栏和 Dock 恢复同一窗口；增加 ⌘, 设置快捷键。
- 启动页显示真实进度，连接过程中不提供重复启动按钮。系统日志只记录握手完成耗时与失败状态，不输出响应内容或凭据。
- 用户已在本机确认第一阶段新构建正常。当前 UI 自动化工具读取此 App 窗口会断开，因此自动化截图和关闭/重开完整交互仍需后续验收；不据进程存在代替这些检查。

旧安装副本保存在本地 `.desktop-artifacts/adaptation/installed-before-stage1.app`。

## 阶段 2A：主要页面层级

- 概览、服务商、搜索与设置使用原生分组 `Form` / `Section`，标题由窗口工具栏统一承载，移除内容区重复大标题。使用系统字体和语义色，保留 SwiftUI Scene 窗口管理。
- 概览参照 Windows 的功能层级，先显示基础配置状态和下一步按钮，再显示能力状态；配置路径、协议信息和路由链折叠收起。未知状态仍明确显示未知，不视为已配置。
- 服务商按三类基础能力分组，每个服务商独立展开，初次显示时展开已有密钥配置的服务商；折叠标题仍显示未保存修改。保存、预览、放弃修改、手动测试和清除密钥的原有行为保留。
- 搜索保留后端工具目录、实验性标注和高级参数；文本参数有常驻字段标签，并可随多行内容扩展。结果复制、导出和来源链接继续保留。
- 设置将语言、配置目录、连接、App 更新及独立 CLI 更新分组；开发路径覆盖收在开发选项中。
- 新增 `mise run desktop:macos:compile`，只编译，不启动界面。新增文案同步至共享语言资源和 SwiftPM 资源。
- 活动与 Skills 页的专门布局调整留待下一小阶段；本次不宣称全部 macOS 适配完成。
- 验证：Release 构建、ShellCheck / actionlint、新增文案双语覆盖和 `git diff --check` 通过；完整 DMG 的签名、架构、安装布局与复制后签名通过；真实后端 12 轮启动/停止/并发状态响应检查通过。未启动新 GUI、未安装替换现有 App，也未继续操作虚拟机。
- 本地 ARM64 产物：`.desktop-artifacts/macos-arm64-20260921T023900Z-77880-2007/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`；SHA-256：`e8ece80e81931cb0f255343c00d120593829aa272f1c7911378b1e48c5763e07`。仍为 ad-hoc 签名测试包，未取得 Developer ID 签名或 Apple 公证。

### 本阶段交给用户的验收点

1. 安装新的测试 DMG，确认正常启动，Dock 和菜单栏图标正常；关闭窗口后从 Dock 或菜单栏恢复窗口。
2. 在概览进入服务商，展开/折叠某项，输入草稿再折叠，确认未保存提示；检查保存、预览和放弃修改。真实服务商测试仍仅在主动点击时发起。
3. 在搜索页选择工具、输入多行问题、展开高级参数，检查布局；使用已有配置自行执行请求，查看结果与来源。
4. 检查设置页、中英文、浅色/深色及缩小窗口后的布局；反馈截图和页面名称。编译与进程检查不能替代这些视觉和交互验收。

### 阶段 2A 反馈：窗口顶部衔接

用户截图指出侧边栏开关、页面标题和侧边栏顶部的衔接不协调。代码检查发现主窗口显式使用 `.windowToolbarStyle(.unified)`；参照 DJOneHub 保留系统默认窗口布局的做法，改为 `.automatic`，让系统根据当前 macOS 和 `NavigationSplitView` 安排原生窗口工具栏。保留侧边栏开关、详情标题、刷新入口和 SwiftUI Scene，不使用固定坐标或自绘标题栏。

此改动只处理窗口样式。构建和打包检查后交由用户重新截图验收，尚不能据编译成功判定视觉问题已消失。

Release 构建、打包静态检查、DMG 签名/安装布局校验和 12 轮真实后端通信检查通过。产物：`.desktop-artifacts/macos-arm64-20260921T025309Z-97384-24312/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`。未运行 GUI 或替换已安装应用。

## 阶段 2B：按 Codex Tweaks 统一布局和内容组织

用户明确指定以 **Codex Tweaks** 为主要布局参照，并要求优化每页内容，不只是换容器。对照本地 `codex_tweaks` 的 `54cca44` 源码：

- `app/Sources/MainWindowView.swift`：平衡分栏、页面标题、概览状态区、左侧说明/右侧操作、带分隔线的包列表。
- `app/Sources/UpdateView.swift`：应用身份区、版本与更新集中在同一操作面。
- `app/Sources/GeneratedPresentationContract.swift`：内容最大宽度 1120、页面边距 32、分区间距 28、卡片内边距 20、连续圆角 14、侧边栏基准宽度 220。

布局提取到 `DesktopLayout.swift`：共用页面标题和内容宽度；主要状态、搜索输入、搜索结果和更新使用少量卡片；能力、服务商与活动列表用分隔线和留白组织。普通说明左对齐，路径可换行，状态文字保持可读对比度。系统仍负责窗口、工具栏、控件和主题。

### 每页调整

| 页面 | 主要内容与操作 | 次要内容 |
| --- | --- | --- |
| 概览 | 状态图标、可用性说明、右侧主要动作；主搜索/文档检索/网页抓取按使用顺序排列 | 更多能力及配置/路由详情折叠 |
| 服务商 | 按三类能力切换，已有配置排在前；保存、预览和放弃修改固定在底部；切换能力仍保留草稿 | 更多服务商、高级字段、字段来源和路由信息按需展开；能力标签与服务商行提示未保存修改 |
| 搜索 | 工具选择、问题输入、执行与独立结果阅读区；query 支持多行；结果顶部提供复制和导出 | 工具说明、高级参数和 JSON 折叠；来源显示域名和可换行地址 |
| 活动 | 进行中的任务在前，其他最近记录在后；状态、耗时和任务操作各有位置 | 目录与清理放入管理菜单；详情窗口可滚动查看长结果和事件 |
| Skills | 来源状态与检查操作同一区；目标名称/状态分列，选择计数、刷新和更新放在目标列表上方 | 检查偏好、文件路径/差异、CLI 环境按需展开；错误和写入回执仍可见 |
| 设置 | 应用图标与版本、偏好行、集中更新操作；独立 CLI 的版本与更新仍单独呈现 | 安装路径、后端开发路径、超时等高级连接选项折叠 |

未修改后端执行、配置保存、付费测试触发、Skills 写入确认和任务取消的权限范围。未知/未观测状态仍如实显示；不把缺少活动视为外部 CLI 空闲。

### 验收边界

不编写 UI 单元测试，不操作主机或 UTM，不安装覆盖现有应用。交付前运行 Release 构建、打包静态检查、语言资源一致性检查与打包流程自带的真实后端通信/签名检查。实际布局、主题、窗口尺寸以及以下交互仍由用户验收：跨能力切换后保存/放弃草稿、搜索并阅读长结果、查看活动详情、选择 Agent 后检查/更新、偏好与更新操作。

上述构建和检查已通过，包括 12 轮真实后端启动/并发请求/停止检查，以及 DMG 内应用和复制安装后的严格签名校验。新增文案全部具备中英文，SwiftPM 资源与打包资源一致。产物：`.desktop-artifacts/macos-arm64-20260921T031320Z-25690-1390/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`，仍为未经 Apple 公证的 ad-hoc 签名测试包。视觉与交互尚未验收，不把构建成功等同于用户验收通过。

### 阶段 2B 反馈：右侧统一背景

用户再次指出顶部侧边栏交界处色块突兀，并要求右侧全部具有白色背景。右侧页面原来使用灰色 `windowBackgroundColor`，卡片使用白色 `controlBackgroundColor`，工具栏又是另一层系统材质。

统一详情容器、滚动页、卡片、工具栏和服务商底部操作区的背景，使用 `textBackgroundColor`（浅色外观为白色，深色外观仍采用系统匹配背景）。详情背景覆盖顶部安全区，工具栏明确使用同一不透明背景，减少标题栏/内容区交界处的色差；内容本身仍遵守安全区。各页的宽度、内容组织和操作行为保留。实际顶部衔接仍需用户截图验收。

用户补充要求将复选框换成开关：在主界面统一使用系统 `.switch` Toggle 样式，覆盖 Agent 选择、Skills/App 自动检查、搜索布尔参数和活动记录选项。Agent 行改为左侧名称/状态、右侧开关，保留无障碍名称、原选择绑定与更新期间禁用状态；开启只表示选中目标，不会自动安装或更新 Skills。

此轮 Release 构建、打包静态检查、12 轮真实后端通信检查、DMG 签名和安装布局检查通过。合并白底与开关调整后的产物：`.desktop-artifacts/macos-arm64-20260921T032358Z-40871-28254/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`。未启动 GUI、未替换现有安装，等待用户验收。

## 阶段 2C：修正 linked-on SDK，恢复新系统原生外观

用户提供了 Codex Tweaks 与 Smart Search 的同屏对比，指出顶部、按钮和开关明显使用不同代际的外观。此前仅调整工具栏样式没有解决核心问题。

直接读取两份可执行文件的 `LC_BUILD_VERSION`：

| 实际二进制 | 最低运行版本 | linked-on SDK |
| --- | --- | --- |
| `/Applications/Codex Tweaks.app` 的两种架构 | 13.0 | 26.5 |
| 上一阶段 Smart Search 打包产物 | 13.0 | **13.0** |
| 本阶段重新链接的 Smart Search | 13.0 | **27.0** |

本机选中的 Xcode SDK 是 27.0，旧 SwiftPM 构建日志也指定了这个 SDK，但最终 Mach-O 错把最低运行版本记成 SDK。系统会依据 linked-on SDK 决定兼容行为；苹果说明新版外观需要用新版 SDK 重建，单纯使用 SwiftUI 并不足够。参见 [Platforms State of the Union](https://developer.apple.com/videos/play/wwdc2025/102/) 和 [Adopting Liquid Glass](https://developer.apple.com/documentation/technologyoverviews/adopting-liquid-glass)。

- 新增 `compile-macos.sh`，从选中的 Xcode 读取实际 SDK 路径和版本，同时用于编译与链接的 `-platform_version`；不修改成品二进制，不伪造 SDK 版本，也不提高最低运行版本。
- 本地 compile 和正式打包共用此入口；低于 SDK 26 时清楚报错。历史 tag 构建时一起加载该脚本。
- DMG 校验新增真实 Mach-O SDK 检查：SDK 至少为 26、与构建工具链一致，最低版本与 Info.plist 一致。已使用上一阶段真实旧产物验证检查会拒绝 SDK 13；新产物 SDK 27/最低系统 13 检查通过。这是产物元数据校验，不是 UI 单元测试。
- 移除强制不透明工具栏背景、覆盖顶部安全区的白底及冗余窗口工具栏样式声明，恢复与 Codex Tweaks 相同的系统窗口管理。仅内容区保留用户要求的白底，开关仍为原生 `.switch`。
- 没有加入自绘阴影或模拟玻璃；按钮、开关、侧边栏和工具栏交由系统按真实 SDK 绘制。实际外观依旧由用户验收，不操作桌面或虚拟机。

本阶段完整 Release/DMG 构建、ShellCheck/actionlint、12 轮真实后端通信、严格签名和新 SDK 校验通过。产物：`.desktop-artifacts/macos-arm64-20260921T033726Z-59779-6926/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`。包内程序实际为 SDK 27.0 / 最低 macOS 13.0。仍为 ad-hoc 签名、未经 Apple 公证的测试包。

## 阶段 3：按使用任务重构六个页面

本阶段用户重新明确授权在本机调试，并要求按 Apple 原生工具的层级组织全部页面。此前“不操作本机”的约束已被这条新指令替代。沿用 Codex Tweaks 的原生分栏和克制的视觉语言，保留 SwiftUI Scene、新 SDK 链接、系统工具栏及浅色白色内容背景。

### 信息与操作的分层

| 页面 | 一级页面 | 二级页面或临时面板 |
| --- | --- | --- |
| 概览 | 基础配置状态、下一步、三类基础能力 | 更多能力保留整行展开；配置路径、版本与路由链移到配置详情面板 |
| 服务商 | 能力筛选、名称搜索、配置状态与草稿提示 | 点击列表进入单个服务商；连接/高级参数分段切换；通用配置与路由有独立入口；字段说明、来源和有效值放入信息浮层 |
| 搜索与研究 | 输入/结果分段切换，工具选择与必要输入 | 可选参数进入搜索选项面板；结果页保留复制、导出、来源和 JSON 详情 |
| 活动 | 左侧记录，右侧详情；全部/运行中/失败筛选 | 观察目录和记录偏好放入设置面板；清除历史继续确认；暂停记录或目录读取失败在一级页面提示 |
| 更新 Skills | 技能来源、各 Agent 状态和开关；固定底部更新按钮 | 文件详情、检查偏好、运行环境各用专门面板；安装确认和真实写入回执保留 |
| 设置与关于 | 通用/App 更新/独立 CLI/高级四类切换 | 运行环境用专门面板；开发选项、安装位置和原始日志保留按需展开 |

保留的展开行使用真正的 SwiftUI `Button` 覆盖整行标签与空白区域，支持键盘激活、展开状态无障碍说明和减少动态效果偏好。仅用于补充内容，不再承担主导航。二级编辑页共享草稿；底部保存/放弃/检查入口始终可见。配置检查单独显示结果，不隐式保存、不发起服务商请求。

布局统一为 24 pt 页面边距、24 pt 分区间距、920 pt 内容宽度上限；少量状态面板使用 12 pt 连续圆角和细分隔线。按钮、开关、选择器、导航、sheet 及弹出信息仍由系统绘制，不增加装饰性阴影或模拟玻璃。

### 功能与验证边界

- 高级配置保留后端未描述的新分区，避免新增字段无入口；能力筛选读取字段实际能力集合。
- 活动详情异步返回时检查当前选择，防止快速切换记录后显示上一条结果；读取失败在详情中呈现错误。
- 新静态文案同步中英文资源，不编写 UI 单元测试。后端协议、草稿保存语义、实验性工具标记和操作确认流程保持兼容。
- 完整 ARM64 Release/DMG 构建、12 轮真实后端启动/并发请求/关闭、严格签名、复制安装后后端启动、真实 SDK 27.0 / 最低 macOS 13.0、语言资源一致性与打包静态检查通过。
- 本机调试采用独立的 `Smart Search Preview.app`，不覆盖现有安装。原生读取工具早期发生 helper 管道断开；最新尝试明确返回 Mac 锁屏，已请求用户解锁。尚无本阶段新界面截图和操作通过证据，不能把编译或静态审查视为视觉验收。
- 当前测试产物：`.desktop-artifacts/macos-arm64-20260921T044020Z-45165-30213/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`，仍为 ad-hoc 签名、未经 Apple 公证的测试包。上游 PR 等用户验收后再创建。

### 独立收尾审查

使用 Impeccable 收尾审查与 simplify 整理此次修改。独立审查发现的三项流程问题已修复并复核为 resolved（源码）：活动来源/阶段/失败类型摘要恢复；列表刷新同步或清理选中详情，刷新时保留已读结果；返回搜索页时恢复已有结果页签。最终重建再次通过 12 轮真实后端通信及完整 DMG 校验。

审查最终 disposition 为 **fix**：源码问题已清除，但视觉验收仍 blocked。最后一次本机读取仍明确返回锁屏；尚未取得新界面的截图、最小窗口、浅深色、英文及键盘/整行点击的实际验证证据。构建产物可供下一步验收，不能据此宣布全部 macOS 适配结束。

## 阶段 4：分栏编辑、置顶项目信息与即时语言刷新

用户要求把 Tab 与二级编辑改成左右分栏，并提供 Codex Tweaks 关于页作为项目信息置顶的参考；另外修复语言切换后部分控件仍需切页才更新的问题。

- 服务商：左侧列表按名称或能力查找，右侧直接编辑当前条目。连接与高级字段按标题分组，共享一个测试区；高级配置和路由也在左侧选择。底部保存/检查/放弃仍作用于全部草稿。选择使用稳定 ID，过滤不清草稿、不自动切换到另一条目。
- 搜索：左侧选择工具、填写请求和执行，右侧持续显示结果或真实运行状态。两侧独立滚动；结果使用其实际运行工具名称，不会随左侧切换而被重新标记。复制和导出在窄栏自动改为纵向排列。
- 设置：应用图标、Smart Search 名称、版本、仓库名称和 GitHub 链接固定在分栏上方；下方左侧选择类别，右侧显示选项。语言和配置目录控件改为适合窄栏的纵向组。
- 共用原生 `HSplitView`，分隔条可拖动。分栏内边距 16 pt，外部不重复套滚动页。服务商最小列宽 176/400、设置 152/400、搜索 280/360，保留原来的 920×620 最小窗口尺寸。
- 语言偏好变化时重建原生控件的展示身份，并同步 SwiftUI locale；服务商与设置选择保留在父视图，配置草稿、搜索输入和运行状态保留在 AppModel。系统菜单由独立观察模型的 Commands 更新，菜单栏内容也使用语言身份。
- 运行标题保存命令/服务商 ID，按当前语言重新取词；离线切换语言时重新解析缓存字段与命令说明，不伪造后端读取时间。实际结果内容和用户输入保持原值。

### 检查与验收范围

Release/DMG 构建、12 轮真实后端生命周期与并发响应、签名、复制安装后启动、SDK 27.0/最低 macOS 13.0、语言资源一致性和打包静态检查均通过。额外在隔离配置目录对真实后端执行中文→英文→中文，确认目录文案更新且 generation/config revision 不变。未新增 UI 单元测试。

独立布局源码核对未发现本次范围内需要修复的实质问题；三处分栏总最小宽度（含分隔条）约 577/553/641 pt，加主侧栏最大 244 pt 均处于 920 pt 约束内。静态尺寸计算不能替代真实控件布局验证。

本机已启动独立 Smart Search Preview；读取窗口的 CUA 工具先超时，再出现 `Sky Computer Use native pipe closed before response`。应用与后端进程仍在运行，但未取得可用的新版截图。实际英文/窄窗口/主题，以及即时语言切换和草稿保留的操作验收仍待完成，不能称为视觉通过。

本阶段产物：`.desktop-artifacts/macos-arm64-20260921T052759Z-7740-22532/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`。仍为 ad-hoc 签名、未经 Apple 公证的测试包；上游 PR 等用户验收后创建。

## 阶段 5：稳定分栏位置与连续设置页

用户实机截图显示：`HSplitView` 的理想宽度没有成为实际初始宽度，搜索输入栏接近 420 pt；不同页面的主侧栏也有宽度变化。设置页顶部信息卡横跨两栏，下方再分成类别和选项，形成割裂的第三列。用户随后明确要求设置回到一个连续页面，以分组区分内容，并补齐 CLI 块的边框。

- 主侧栏固定 220 pt，保留原生隐藏按钮、标题栏、工具栏和 SwiftUI Scene。语言变化只重建列表和详情展示，不重建外层导航容器；分栏页面重建时恢复自己的宽度。
- 服务商、搜索与活动统一使用 `DesktopSplitView`，内部为 `NSSplitViewController` 和两个持久 hosting controllers。显式设置初始位置，按页面保存宽度，切换条目只更新内容。hosting view 禁用内容驱动的尺寸约束，明确传递 locale、colorScheme 和共用原生控件样式。
- 三页左栏初始宽度分别为 220、320、260 pt；详情优先吸收窗口尺寸变化。左／右 holding priority 为 251／250，低于原生分隔条拖动优先级 490。首轮实机暴露 750 会阻止初始定位，已经纠正。初始位置以实际布局确认；窗口临时收窄不会覆盖更宽的保存偏好。参见 [NSSplitView holding priority](https://developer.apple.com/documentation/appkit/nssplitview/setholdingpriority(_:forsubviewat:)) 与 [setPosition](https://developer.apple.com/documentation/appkit/nssplitview/setposition(_:ofdividerat:))。
- 搜索把工具与说明、输入字段、运行与选项分别成组，组内使用紧密间距，结果区域获得更多空间。运行和选项在窄栏可纵向排列，保留原执行与草稿语义。
- 设置的项目信息、GitHub、通用、App 更新、独立 CLI、高级设置位于同一滚动内容区；标题、32 pt 章节间距和细边框面板区分层级。语言／配置目录采用左说明、右控件的行布局。CLI 版本与更新、运行环境、App 内置 CLI 均有完整面板，原路径、日志、更新、超时、环境准备及启用确认入口保留。

### 实机证据与限制

使用独立 `Smart Search Layout Preview` 调试，没有覆盖 `/Applications` 的用户安装。CUA 在用户协助切到搜索页后恢复过一段时间，取得搜索、服务商、活动及早期设置布局的实际截图。

| 操作 | 观察结果 |
| --- | --- |
| 首次进入三处分栏 | 搜索 320、服务商 220、活动 260；主侧栏 220 |
| 原生 AX 调整搜索栏至 348 | 切到在线研究、离开返回、隐藏再显示主侧栏，均保持 348 |
| 原生 AX 调整服务商栏至 244 | context7 切到字段更多的 openai-compatible，保持 244 |
| 原生 AX 调整活动栏至 276 | 切换筛选、原生窗口放大及恢复，保持 276 |
| 窗口缩放的临时布局日志 | 内容宽度 860 → 2340 → 860，活动左栏保持 276；临时日志代码已移除 |

坐标拖动未产生实际位置变化，因此不能称鼠标拖动验收通过；上表采用的是原生 AX 分隔条控件的调整。最窄窗口夹紧后恢复、最终中英文切换和深色外观仍需验收。进入最终连续设置页时，读取工具反复出现 `Sky Computer Use native pipe closed before response`；这是观察工具故障，尚未取得最终设置页的完整截图，不等于 App 崩溃，也不能据编译通过宣称视觉通过。

独立布局审查与 simplify 收尾已完成：修复保持优先级、初始化确认和自动夹紧保存问题，移除设置分类导航与重复 CLI 标题，保留所有设置入口。最终布局扫描无机械发现，扫描不证明 SwiftUI 的实际视觉质量。

Release/DMG 构建、12 轮真实后端启动／并发状态请求／停止、严格签名、复制安装后的后端启动、SDK 27.0／最低 macOS 13.0、语言资源一致性与打包静态检查通过。未编写 UI 单元测试。产物：`.desktop-artifacts/macos-arm64-20260921T061258Z-68964-14325/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`，仍为 ad-hoc 签名、未经 Apple 公证的测试包；上游 PR 等用户验收。

## 阶段 6：按路由模式配置与已有密钥显示

用户要求意图路由位于服务商栏最上方，随后明确为无图标的纯文字固定入口。该入口使用原生列表行，独立于下方搜索框和滚动服务商列表；首次进入服务商页面默认选择它。原有服务商、高级配置与路由诊断入口保留。

路由选项显示中文名称，在英文界面使用对应英文；保存值仍为原后端协议值。表单立即根据草稿中的模式切换，已有参数按原草稿保存逻辑保留。

| 模式 | 显示内容 |
| --- | --- |
| 混合路由 `hybrid` | 向量模型、分类模型、共用路由超时；说明模型按配置启用 |
| JEV 语义路由 `jev` | TypeSafe 连接、检索与判断、结果处理；仅开启过滤时显示过滤阈值 |
| 规则路由 `rules` | 本地规则说明，无模型参数 |
| 关闭路由 `off` | 关闭说明，无路由参数 |

两项通用研究数据源偏好移到高级配置中的独立入口。JEV 汇总选项也使用可读名称；布尔配置使用原生开关。枚举选择器直接显示当前有效值，选择原值会回到无草稿状态，未知值保留可见回退。

已有 Key 以固定圆点掩码显示在 SecureField 提示中，输入新值仍使用安全输入控件；掩码从不写入配置草稿。显式清除后恢复输入提示，未配置的 Key 显示“未配置”。环境变量提供的只读 Key 同样使用固定掩码。后端旧掩码逻辑会把空值和短 Key 都显示为 `***`，因此状态接口新增只含布尔值的 `secret_presence`，原生界面据此判断是否已有有效值；旧后端仍有兼容回退。

### 验证

- 在独立 Routing Preview 中实际切换 JEV、规则、关闭、混合，再恢复开始时的 JEV 草稿。截图和控件树确认模式参数切换正确，中文选项正确，左栏保持 220。开启／关闭 JEV 过滤时阈值分别出现／消失。未保存这些配置、未调用在线服务，用户原有的一项路由草稿已恢复。
- 最终纯文字固定入口与密钥掩码是在这次实机轮次后补充，已编译与打包，但尚未取得其最终 GUI 截图；运行中的旧 Routing Preview 未覆盖，以免丢失用户未保存草稿。
- 对最终打包后端使用隔离配置目录执行真实 stdio initialize／shutdown，分别验证缺失 Key、空 Key、短 Key、正常 Key 的存在标志，并确认响应中没有原始示例密钥。
- 完整 Release/DMG 构建、12 轮真实后端生命周期、严格签名、复制安装后启动、SDK 27.0／最低 macOS 13.0、语言资源一致性和打包静态检查通过。使用 simplify 整理了模式分组和显示辅助逻辑，未新增 UI 单元测试。

最终产物：`.desktop-artifacts/macos-arm64-20260921T063047Z-94707-5490/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`。保留 ad-hoc 测试签名；用户验收后再创建上游 PR。

## 阶段 7：保留系统 Dock 图标底板

用户发现未运行时图标有白色底板，启动后底板消失。启动委托把透明的原始 `SmartSearch.icns` 读成 `NSImage` 后赋给 `NSApp.applicationIconImage`，覆盖了系统对应用包图标的呈现。现在打包的 `.app` 使用系统从 `CFBundleIconFile` 解析的图标；只有直接运行裸 SwiftPM 可执行文件时才设置备用图标。关于页继续使用品牌资源，未改动原始图稿。

完整 Release/DMG 构建、12 轮真实后端生命周期、严格签名、复制安装后后端启动、SDK 与静态检查通过；额外核对包内 Info.plist 声明的 ICNS 存在、格式有效且与源资源一致。未新增 UI 单元测试。CUA 读取 Dock 超时，运行前后最终视觉一致性仍需实机验收。

产物：`.desktop-artifacts/macos-arm64-20260921T063708Z-4259-31096/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`。

## 阶段 8：按实际能力分类服务商与用途说明

用户要求左栏按类型组织服务商，并把详情标题下方的通用草稿提示改成与程序内部配置一致的用途说明。

- 分组直接读取状态接口已有的 `provider_profiles.capability`，不再从必填字段推断能力。按主搜索、文档检索、网页搜索、网页抓取、垂直检索排列；组内保留已配置优先、名称排序。服务商只出现一次，列表行只保留名称、配置状态和未保存提示。
- Tavily、TinyFish 归入网页搜索；Firecrawl 归入网页抓取。多能力详情从同一运行时 profile 的 `capabilities` 读取，同时说明搜索、抓取或站点地图支持。未来未识别的类型保持可见；旧后端缺少 profile 时使用字段能力回退。
- 右侧从 profile 的 `descriptions` 读取当前界面语言的简介和“擅长／不适合”副介绍，按搜索、抓取和站点地图等操作分开展示；同一份文本的英文版本还用于 Jev 路由判断。实验性、仅明确指定时调用等状态说明继续单独显示。没有在桌面端另建服务商到用途的硬编码表。
- 名称、全部声明能力和用途均可搜索；分类切换保留稳定选择与所有草稿。按用户最新要求，意图路由改为搜索框下方统一滚动列表的第一行，保留纯文字样式，与服务商分类一起滚动；移除独立的固定列表。

### 验证

使用 simplify 整理了新辅助逻辑；Release 编译、完整 DMG 构建、12 轮真实后端生命周期、签名、复制安装后的后端启动、SDK 27.0／最低 macOS 13.0、打包静态检查通过。两份语言资源一致，13 个可编辑服务商均有运行时 profile，所有用途词条均覆盖中英文。

额外对最终打包后端在隔离配置目录执行中文、英文 initialize／shutdown，确认返回的 profile 与源码声明完全一致、能力 ID 不随语言改变，且包内用途翻译完整。关闭更新检查，未发起在线服务商请求；未新增 UI 单元测试。

独立 `Smart Search Providers Preview` 的读取曾在初始窗口断开，随后恢复：截图及控件树确认五类服务商、Context7 用途副标题、已有 Key 掩码均正常显示，内部分栏保持 220 pt。用户随后要求将意图路由放回统一列表首行；确认预览没有未保存修改后正常退出并更新至最终构建，旧 Routing Preview 及其未保存草稿未覆盖。重启后的初始窗口再次触发 `Sky Computer Use native pipe closed before response`，最终列表滚动及英文视觉验收尚未完成。

产物：`.desktop-artifacts/macos-arm64-20260921T070053Z-38018-24627/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`，包含阶段 7 的 Dock 修复。用户验收后再创建上游 PR。

## 阶段 9：紧凑设置面板与开关对齐

用户截图中的自动更新开关使用 `.frame(maxWidth: .infinity)`，默认把标签和开关整体居中。现在该行明确分为可换行的左侧说明和右侧原生开关，保留开关的可访问名称与原有绑定。

- 设置页面留白 28 → 24 pt，章节间距 32 → 24 pt，标题到面板间距 16 → 12 pt。面板保留细边框和清楚的分组层级。
- 共用 `DesktopPanel` 新增可选紧凑模式：水平留白仍为 16 pt，垂直留白 16 → 12 pt，内容间距 12 → 8 pt。仅设置页启用；通用行移除额外垂直留白，标签和说明相隔 4 pt。
- App 与 CLI 版本行、开发选项内容采用 8 pt 间距，高级面板间距收紧至 12 pt。运行环境说明与准备按钮同排；项目信息图标改为 48 pt。

### 验证

Release 编译、完整 DMG 构建、12 轮真实后端生命周期、严格签名、复制安装后的后端启动、SDK 27.0／最低 macOS 13.0、打包静态检查通过。未新增 UI 单元测试。按 simplify 检查并复用共用面板；独立 layout detector 返回空结果，该扫描不证明 SwiftUI 的视觉质量。

为绕开初始概览页的界面读取故障，在忽略的产物目录中单独构建 `Smart Search Settings Preview`：仅调试副本的初始页面和窗口默认宽度改为设置页与 920 pt，其余界面源码与交付版本一致。实际截图确认 App 更新说明靠左、开关靠右，长路径能够换行；App 更新、独立 CLI、运行环境及高级面板的紧凑布局已观察。界面显示已有的下载完成状态；检查过程中未切换自动更新开关、未触发下载或安装。英文、深色外观与更宽窗口未在本轮实机复核。

产物：`.desktop-artifacts/macos-arm64-20260921T071222Z-54459-8111/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`。独立设置预览已启动，交付包仍按原流程进入概览。

## 阶段 10：用原生浮层替换顶部提示横幅

用户截图中的长提示插在 detail 容器顶部，参与页面布局并与原生标题栏重叠。现在移除这层横幅容器，由页面直接承载导航标题和工具栏；操作反馈改为工具栏中的“操作提示”入口和原生 popover。

- 工具栏入口始终保留，没有消息时禁用。新消息在工具栏下方展开，不改变标题栏、侧栏或页面的位置；关闭后可以再次查看，点击“清除提示”才移除消息。
- 完整正文可以选取，正文宽 360 pt、外留白 16 pt；超过 320 pt 时在浮层内部滚动，标题和清除按钮保持可见。错误带警示标题，普通说明不再统一标为绿色成功状态。
- 主动操作连续产生同一错误时，通过独立呈现 ID 再次提示；普通消息同样支持相同文本重复反馈。活动记录和选中详情的后台刷新贯穿使用 `repeatFeedback: false`，相同轮询错误不会反复弹出；手动刷新和主动选中详情仍会反馈失败。
- 新入口、工具提示和清除操作补齐中英文资源，两份词库保持一致。

### 验证

完成 simplify 整理与独立布局审查；修复审查发现的相同错误无法再次提示、活动详情轮询重复提示两个问题。原生布局扫描未报告发现，该静态结果不替代实机验收。未新增或运行 UI 单元测试。

在忽略的产物目录构建独立 `Smart Search Feedback Preview`，使用最终界面源码。调试副本仅将初始页设为设置、默认宽度改为 920 pt，并加入明确标注“界面预览”的示例消息菜单；这些入口不进入交付包，也不执行示例文案描述的下载、安装、复制或保存配置操作。

中文、浅色实机控件树和截图确认：截图中的长安装说明在工具栏下方打开，页面位置保持稳定；关闭后可通过入口重看，清除后入口禁用；相同错误和普通消息在再次触发后重新出现；30 段文字只滚动浮层正文，标题、清除按钮和主页面滚动位置不变。英文、深色外观及更宽窗口未在本轮实机复核。

最终 Release 编译、完整 DMG 构建、12 轮真实后端启动／并发响应／关闭、严格签名、复制安装后的后端启动、安装布局及 SDK 27.0／最低 macOS 13.0 检查通过。产物：`.desktop-artifacts/macos-arm64-20260921T073813Z-91404-31744/SmartSearch-0.1.22-macos-arm64-unsigned-test.dmg`。该包仍为 ad-hoc 签名、未经 Apple 公证的测试包；交付包按原流程进入概览，上游 PR 等待用户验收。

## 上游同步：v0.1.23

将 `origin/main` 的 `b5773df`（`v0.1.23`）合并到适配分支，纳入上游透明图标、Windows 自签名和发布验签流程。唯一文本冲突位于桌面构建说明，已同时保留 Windows 签名流程与 macOS ad-hoc／未公证的说明；中英文 App 指南同步使用准确的 macOS 签名表述。此前 macOS 界面源码、SDK 链接、DMG 校验和 `.DS_Store` 忽略保持完整。

桌面更新、发布工作流、品牌资源和后端运行时共 41 项现有测试通过，打包静态检查通过。合并后重新构建的 arm64 DMG 通过 12 轮真实后端生命周期、严格签名、安装布局、复制安装后的后端启动和 SDK 27.0／最低 macOS 13.0 校验。Windows 实际签名构建及本轮 GUI 外观未重新验证。

产物：`.desktop-artifacts/macos-arm64-20260921T082224Z-49608-24562/SmartSearch-0.1.23-macos-arm64-unsigned-test.dmg`。这是未公证测试包；本次同步不创建上游 PR。
